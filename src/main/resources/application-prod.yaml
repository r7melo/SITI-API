spring:
  application:
    name: SITI-API

  datasource:
    url: jdbc:mysql://db:3306/siti_db
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false